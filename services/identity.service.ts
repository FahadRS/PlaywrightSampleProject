import { Driver } from "../Driver";

export class IdentityService {

    /* this method login the user if the user is not already logged in  */
    public static async login(){
        //await Driver.waitForSelector();
    }

    /* confirm email address on the current portal */
    public static async confirmEmailAddress() {

    }
}

